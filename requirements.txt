torch>=2.0.0
torchvision
matplotlib
seaborn
pandas
numpy
psutil
tqdm
# The real implementation would require Triton, but we keep it optional to
# avoid installation issues on CPU-only test runners.
triton>=2.1; platform_system != "Windows"